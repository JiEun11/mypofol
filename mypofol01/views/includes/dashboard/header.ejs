<div class="content-header">
    <div class="content-header-inner">
        <div class="profile-pic">
            <a href="#">
                <img src="<%=req.session.authUser.profileImage %>">
            </a>
        </div>
        <div class="profile-name">
            <h1>
                <a href="/<%=req.session.authUser.accountName %>" class="color-fg-default">
                    <%=req.session.authUser.accountName || req.session.authUser.profileName  %>
                    <% if(req.session.authUser.accountName) { %>
                    <span>(<%=req.session.authUser.profileName  %>)</span>
                    <% } %>
                </a>
            </h1>
            <div class="d-flex flex-items-center flex-wrap" style="color: var(--fgColor-accent, var(--color-accent-fg))">
                <p class="color-fg-muted mb-0 mr-1">
                    Your personal account
                </p>
            </div>
        </div>
    </div>
    <a href="/<%=req.session.authUser.accountName %>" class="btn">Go to my portfoio</a>
</div>