use('sample-airbnb');

// ex1) 'Fabian' 이라는 reviewer의 comment 중에 'best', 'worst' 라는 단어가 들어간 것을 검색(/.../(i)gnoreCase, 대소문자 구별 없음)
db.listingsAndReviews.find({
    reviews: {
        $elemMatch: {
            reviewer_name: "Fabian",
            comments: /best|worst/i
        }
    }
})